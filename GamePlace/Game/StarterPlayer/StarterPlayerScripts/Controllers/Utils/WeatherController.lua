local Knit = require(game.ReplicatedStorage.Packages.Knit)

local WeatherController = Knit.CreateController({
	Name = "WeatherController",
})

function WeatherController.KnitStart() end

return WeatherController
